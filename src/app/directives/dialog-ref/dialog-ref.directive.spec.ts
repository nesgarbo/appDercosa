import { DialogRefDirective } from './dialog-ref.directive';

describe('BaseDetailDirective', () => {
  it('should create an instance', () => {
    // const directive = new DialogRefDirective();
    // expect(directive).toBeTruthy();
  });
});
