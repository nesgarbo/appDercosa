import { RequiredFieldDirective } from './required-field.directive';

describe('RequiredFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredFieldDirective();
    expect(directive).toBeTruthy();
  });
});
