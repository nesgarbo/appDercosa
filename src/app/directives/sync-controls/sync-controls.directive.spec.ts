import { SyncControlsDirective } from './sync-controls.directive';

describe('SyncControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new SyncControlsDirective();
    expect(directive).toBeTruthy();
  });
});
