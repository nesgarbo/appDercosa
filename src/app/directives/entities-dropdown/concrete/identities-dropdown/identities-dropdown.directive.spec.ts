import { IdentitiesDropdownDirective } from './identities-dropdown.directive';

describe('IdentitiesDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentitiesDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
