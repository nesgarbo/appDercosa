import { BrokersDropdownDirective } from './brokers-dropdown.directive';

describe('BrokersDropdownDirective', () => {
  it('should create an instance', () => {
    // const directive = new BrokersDropdownDirective();
    // expect(directive).toBeTruthy();
  });
});
