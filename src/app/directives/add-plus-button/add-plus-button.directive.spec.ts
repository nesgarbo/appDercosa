import { AddPlusButtonDirective } from './add-plus-button.directive';

describe('AddPlusButtonDirective', () => {
  it('should create an instance', () => {
    // const directive = new AddPlusButtonDirective();
    // expect(directive).toBeTruthy();
  });
});
